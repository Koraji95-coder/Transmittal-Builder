@copilot Please also bump the starting version to `4.0.0` as part of this PR, since you're already touching `tauri.conf.json`. This is a brand-new app line (the prior v2 was a different codebase); starting at 4.0.0 keeps the version history sensible for end users.

Update **all three** of these files so they stay in lockstep — the updater reads the installed version from `CARGO_PKG_VERSION`, so if they drift the update check will misbehave:

1. `frontend/src-tauri/tauri.conf.json` → `"version": "4.0.0"`
2. `frontend/package.json` → `"version": "4.0.0"`
3. `frontend/src-tauri/Cargo.toml` → `version = "4.0.0"`

Also update any example `latest.json` snippets in `RELEASING.md` and `README.md` to use `4.0.0` / `Transmittal-Builder_4.0.0_x64-setup.exe` so the docs match what the first real release will look like.

No other behavior changes — just the version bump alongside the splash work.